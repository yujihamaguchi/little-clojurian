# estie コーディング試験 自習プラン（新規混在・確定版）

最終更新: 2026-06-07(日) 16:29
試験日: 2026-06-16(火) 13:30-15:00 / 東京ミッドタウン・イースト4F / 解答言語 Clojure

## 進捗
- 消化済み(ソート肩慣らし): Q036 my-insert, Q037 isort, Q042 qsort
- 消化済み(新規): **Q207** 契約→テナント→ビル名（naive 線形検索 / fast idマップ前処理の2版）
  - 到達点: naive=二重ループ O(N²)、fast=マップ構築 O(T+B)+各引き O(1) の足し算で O(N)。「O(N²)→O(N)」を口頭で言える状態
- 消化済み(新規): **Q208**（6/8）ネスト→フラット flatten-buildings（単一 for＋生成器3つ）
  - 到達点: 階層走査なので直積でなく **O(N)**（葉=テナント総数を1回ずつ）。「ネスト for でも内側が外の子なら掛け算でなく足し算」を口頭で
- 学んだ周辺知識: namespaced keyword(:building/id)、some vs filter+first、find-first はコア無し→(first (filter…))、transducer/comp の向き、defrecord 再定義の instance? 問題、:refer-clojure :exclude

### 6/7(日) 振り返り
- 大ブロックだったが **Q207 のみ**消化（概念の深掘りに時間を使った）。Q208/Q209/Q214 は未着手 → 平日昼に繰り越し
- 残りの ★必須3問（Q208, Q209, Q214）を最優先で 6/9〜6/10 に再配置

## 残り稼働日
| 日 | 使える時間 | 用途 |
|---|---|---|
| 6/7(日) | 済 | Q207 のみ消化 |
| 6/8(月) | — | upsider 面談（estie準備しない）／Q208 は消化済 |
| 6/9(火) 昼 | ~45分 | ★Q209, ★Q214（Q209 は Q208 が新鮮なうち隣接で） |
| 6/10(水) 昼 | ~45分 | Q213＋clojure.set 思い出し |
| 6/11(木) 昼 | ~45分 | Q205, Q210, Q215 |
| 6/12(金) 昼 | ~45分 | Q216, Q219, Q220 |
| 6/13(土)・14(日) | — | なし |
| 6/15(月) 昼 | ~45分 | 最終確認のみ（新規なし） |
| 6/16(火) | — | 試験 13:30 |

## 優先度（必須は達成レベルで2段）
- **必須A 空で書ける**: Q207(済), Q208, Q209, Q214 … スラスラ手書きできるまで
- **必須B 口頭で方針を言える**: Q219, Q220 … コードは詰まってOK。グラフは純粋な穴なので「訪問済み set＋隣接探索／BFS は PersistentQueue で距離更新」を説明できる状態に（estie の C/D 割り切り方針に乗せる）
- **準必須**: Q213（必須Aと同じハッシュ family。低コストで引き出し増）
- **やれれば（領域A/B本体）**: Q205, Q210, Q215, Q216, Q206
- **余裕があれば**: Q221, Q211, Q212, Q217, Q218, Q222, Q223

---

## 日割り（6/7 のスリップを反映して再配置）

> 凡例: ★ = 必須(Tier1「空で書ける」)。太字は単なる強調で必須の印ではない。
> 必須は Q207/Q208/Q209/Q214 の4問。うち Q207 は消化済 → 残り必須は ★Q208 / ★Q209 / ★Q214 の3問。

### 6/9(火) 昼 — 必須2問（最優先）
- ★ **Q209** フラット→ネスト（group-by 2段）**Q208 の逆**。昨日の Q208 が新鮮なうちに対で固める。group-by 2段の骨格を暗記
- ★ **Q214** two-sum（O(n²) vs seenマップ）最頻出（二重ループ O(n²) → 1パス＋seenマップ O(n)）

### 6/10(水) 昼 — 領域A（clojure.set 思い出し）
- 【肩慣らし clojure.set】Q067 rename / Q068 select / Q069 project / Q070-071 join / Q072 合成
  → 「テーブル結合＝set/join」「射影＝project」「選択＝select」を1〜2分で思い出す。Q213 の集合演算に直結
- **Q213** 2リストの共通テナント（二重ループ vs ハッシュ集合 / set/intersection）

### 6/11(木) 昼 — 領域B：集約・結合
- **Q205** 各ビルの空室面積合計（ネスト2段集約）
- **Q210** エリア×ビルの2軸集計（group-by / reduce で多軸）
- **Q215** 掲載にビル名付与（idマップ結合）※時間あれば

### 6/12(金) 昼 — 必須B グラフ（口頭で方針を言える状態に）
- ★口 **Q219** 経路の有無（DFS/BFS、隣接マップ）… 訪問済み set ＋ 隣接探索
- ★口 **Q220** BFS最短距離（PersistentQueue、距離マップ）… キューで距離更新
- **Q216** 重複掲載の検出（同一(building-id,floor)を group-by で O(n)）※時間あれば

### 6/15(月) 昼 — ★最終確認のみ（新規なし）
- Q207/208/209/214 を空で書けるか最終チェック
- REVIEW.md 末尾「口頭で必ず言える定型句」を一通り
- Clojure固有の計算量の癖（永続ベクタ末尾O(1)、concat/into、遅延seq）を口頭で

---

## 取捨選択（スリップ後の現実版）
- **必達**: ★Q208, Q209, Q214（6/9-10）＋ 領域A Q213
- **やれれば**: Q205, Q210, Q215, Q216, Q219, Q220
- **捨ててよい**: Q206, Q212(口頭のみスキマで), Q221(D), Q211, Q217, Q218, Q222, Q223
  - Q212 は「素マップ/defrecord/defprotocol の使い分け」を口頭で言えれば十分（コード不要）
  - C/D が出ても「考え方を説明できればコード詰まってOK」の方針は維持

## チェックリスト
- [x] Q036 / [x] Q037 / [x] Q042
- [x] **Q207**（naive+fast、O(N²)→O(N) 理解済み）
- [x] **Q208**（単一 for、O(N) 理解済み）
- [ ] Q214  ← ★6/9
- [ ] Q209 / [ ] Q213（＋clojure.set Q067-072 思い出し）  ← ★6/10
- [ ] Q205 / [ ] Q210 / [ ] Q215  ← 6/11
- [ ] Q219(口頭) / [ ] Q220(口頭) / [ ] Q216  ← ★必須B 6/12
- [ ] 最終確認（空書き4問＋グラフ口頭＋定型句）  ← 6/15
