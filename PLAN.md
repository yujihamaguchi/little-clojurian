# estie コーディング試験 自習プラン（新規混在・確定版）

最終更新: 2026-06-13(土) 14:17
試験日: 2026-06-16(火) 13:30-15:00 / 東京ミッドタウン・イースト4F / 解答言語 Clojure

## 進捗
- 消化済み(ソート肩慣らし): Q036 my-insert, Q037 isort, Q042 qsort
- 消化済み(新規): **Q207** 契約→テナント→ビル名（naive 線形検索 / fast idマップ前処理）
  - naive=二重ループ O(N²)、fast=マップ構築 O(T+B)+各引き O(1) で O(N)。「O(N²)→O(N)」を口頭で言える
- 消化済み(新規): **Q208** ネスト→フラット flatten-buildings（単一 for＋生成器3つ）
  - 階層走査なので直積でなく **O(N)**。「ネスト for でも内側が外の子なら掛け算でなく足し算」
- 消化済み(新規): **Q209** フラット→ネスト nest-by-building（group-by 2段＋set/rename-keys、O(N)）
  - flatten=「グループ展開＋リネーム」、nest=「グループ集約＋リネーム復元」。リネームは省けない本質
- 一部のみ: **Q214 naive**（O(n²)、`:tenant-id` 大小でデデュープ＋first 短絡）。**fast(seen マップ) 未着手**
  - 補足: fast の seen マップは memoize とは別物。memoize=計算結果のキャッシュ / seen=見たデータの索引（補数を O(1) 照会）
- 学んだ周辺: namespaced keyword、some vs filter+first、find-first はコア無し→(first(filter…))、transducer/comp の向き、defrecord 再定義の instance? 問題、:refer-clojure :exclude、set/rename-keys、`->` 二重引数の罠、memoize と seen マップの違い

## ★現状サマリ（6/13 時点）
- 必須A: Q207✅ Q208✅ Q209✅ / **Q214 は naive のみ（fast 未）**
- 必須B グラフ: Q219 / Q220 **未着手**（6/12 昼スリップ）
- 準必須 Q213 / clojure.set 復習: **未**
- 教訓: 6/7 大ブロックが Q207 深掘りで溶けた → **6/14 はリスクの高いグラフを最初に**

## 残り稼働日
| 日 | 使える時間 | 用途 |
|---|---|---|
| 6/13(土) 今 | 余力あれば30分 | あれば Q214 fast を即消化（無ければ 6/14 へ） |
| 6/14(日) | **2〜3h ★最重要ブロック** | リスク順: ①Q214 fast ②グラフ Q219/220 ③Q213 |
| 6/15(月) 昼 | ~45分 | 最終確認のみ（新規なし） |
| 6/16(火) | — | 試験 13:30 |

## 優先度（必須は達成レベルで2段）
- **必須A 空で書ける**: Q207✅ Q208✅ Q209✅ / **Q214 fast（残り・最優先で書く）**
- **必須B 方針を言える**: Q219, Q220 … 最低「口頭で方針」、時間あれば実装
- **準必須**: Q213（＋clojure.set 思い出し）
- **ボーナス（やれれば）**: Q205, Q210, Q215, Q216, Q206
- **余裕があれば**: Q221, Q211, Q212, Q217, Q218, Q222, Q223

---

## 日割り

### 6/13(土) 今 — 余力があれば1つだけ
- **Q214 fast**（seen マップの loop/recur）。10分で書ける。書ければ**必須A 完了**。無理なら 6/14 冒頭へ

### 6/14(日) ★大ブロック 2〜3h（リスクの高い順に上から。深掘りで溶かさない）
1. **Q214 fast**（未なら最初に。必須A完了）
2. **★必須B グラフ Q219 → Q220（最優先）**
   - Q219 経路の有無: 訪問済み set ＋ 隣接探索（DFS/BFS）
   - Q220 BFS最短距離: PersistentQueue ＋ 距離マップ
   - まず「口頭で方針」を固める → 時間があれば実装まで
3. **Q213** 2リストの共通テナント（ハッシュ集合 / set/intersection）＋ clojure.set 思い出し（Q067-072）
4. 余裕があれば: 必須4の空書き点検 / 領域B Q205・Q210 を軽く
   ※質優先。詰まったら深追いせず方針説明できる状態で次へ

### 6/15(月) 昼 — ★最終確認のみ（新規なし）
- Q207/208/209/214 を空で書けるか最終チェック
- グラフ Q219/220 の方針を口頭で言えるか
- REVIEW.md 末尾「口頭で必ず言える定型句」を一通り
- Clojure固有の計算量の癖（永続ベクタ末尾O(1)、concat/into、遅延seq）を口頭で

---

## 取捨選択（6/13 スリップ後・現実版）
- **必達**: Q214 fast ＋ グラフ Q219/220（口頭）＋ Q213
- **6/14 で狙えれば**: グラフ実装格上げ／必須4空書き点検
- **捨てて構わない**: 領域B本体(Q205/210/215/216)、Q221, Q206, Q212(口頭のみ), Q211, Q217, Q218, Q222, Q223
  - C/D が出ても「考え方を説明できればコード詰まってOK」の方針は維持

## チェックリスト
- [x] Q036 / [x] Q037 / [x] Q042
- [x] **Q207** / [x] **Q208** / [x] **Q209**
- [x] **Q214**（naive＋fast 済。必須A 完了）
- [ ] Q219(口頭) / [ ] Q220(口頭)  ← ★必須B
- [ ] Q213（＋clojure.set Q067-072 思い出し）  ← 準必須
- [ ] 最終確認（空書き4問＋グラフ口頭＋定型句）  ← 6/15
