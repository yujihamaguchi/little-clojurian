# estie コーディング試験 自習プラン（estie ブログ準拠版）

最終更新: 2026-06-13(土)
試験日: 2026-06-16(火) 13:30-15:00 / 東京ミッドタウン・イースト4F / 解答言語 Clojure
出典: estie 公式ブログ「コーディング試験について」(2025-03-14)

## estie が実際に評価する3軸（ブログより）
1. **改善を重ねる力** … まず思いついた実装→改善していくプロセス（naive→fast）
2. **思考の深さ** … なぜその実装か／テストケース／**計算量を説明できるか**
3. **曖昧性への対処** … 曖昧な要件を整理し、必要な情報をヒアリングできるか

重要な前提（ブログ明記）:
- **題材は不動産ドメインの実務寄り問題。競技プログラミングではない**
- **グラフ・DP 等の特定アルゴリズム分野への言及なし** → これらは実装ドリル不要、口頭 awareness で十分
- 「不明点は遠慮なく相談」「面接官は敵でない」→ 対話前提

## 進捗（核心は済・3軸の①②をほぼ充足）
- 消化済み(ソート肩慣らし): Q036 my-insert, Q037 isort, Q042 qsort
- 消化済み(新規): **Q207** 契約→テナント→ビル名（naive 線形検索 / fast idマップ前処理）
  - naive=二重ループ O(N²)、fast=マップ構築 O(T+B)+各引き O(1) で O(N)。「O(N²)→O(N)」を口頭で言える
- 消化済み(新規): **Q208** ネスト→フラット flatten-buildings（単一 for＋生成器3つ）
  - 階層走査なので直積でなく **O(N)**。「ネスト for でも内側が外の子なら掛け算でなく足し算」
- 消化済み(新規): **Q209** フラット→ネスト nest-by-building（group-by 2段＋set/rename-keys、O(N)）
  - flatten=「グループ展開＋リネーム」、nest=「グループ集約＋リネーム復元」。リネームは省けない本質
- 消化済み(新規): **Q214**（6/13）two-sum。naive=全ペア O(n²)、fast=seen マップで O(n)
  - 照会→挿入の順で自己ペア・重複を自然回避。seen マップは memoize とは別物
- 学んだ周辺: namespaced keyword、some vs filter+first、find-first はコア無し→(first(filter…))、transducer/comp の向き、defrecord 再定義の instance? 問題、:refer-clojure :exclude、set/rename-keys、`->` 二重引数の罠、memoize と seen マップの違い

→ **3軸の①改善プロセス・②計算量説明はすでに強い**（Q207/214 が直球）。残りは「ドメイン操作の幅」と「③曖昧要件の対処（当日の振る舞い）」。

## 優先度（記事準拠＝ドメイン操作 主役、アルゴリズムは口頭のみ）
- **核心（完了）**: Q207✅ Q208✅ Q209✅ Q214✅ … 改善プロセス＋計算量説明の地力
- **主役：ドメインデータ操作の流暢さ**（「業務で発生する問題」のど真ん中）
  - Q205 ネスト集約（ビルごと空室面積合計）
  - Q210 多軸集約（エリア×ビル）… Q205 の自然な後出し
  - Q211 ネスト不変更新 update-in/assoc-in（「フロア2の賃料10%up」型・後出し要件の道具）
- **手に馴染ませる小物**: `(into {} (map (juxt :id identity)) coll)`、`max-key`/`min-key`、`(sort-by f >)`＋`take`、`fnil`、`group-by`、`frequencies`、`are`（表形式テスト `(are [in out] (= out (f in)) …)`）
- **目を通すだけ（口頭）**: Q212 素マップ/defrecord/protocol のモデリング判断
- **口頭 awareness のみ（実装しない・保険）**: グラフ Q219/220、DP Q221、貪欲 Q223
  - 競プロ官がドメインの皮で出す可能性への薄い保険。評価されるのは naive→改善 なので「方針を言える」で十分
- **確認のみ（冗長＝既習と同思想）**: Q213(≒Q214) / Q215(≒Q207) / Q206(≒Q205)
- **skip**: グラフ/DP/貪欲の実装、Q216/Q217/Q218/Q222、空書きチェック、対話練習

---

## 6/14(日) 3時間 — 時間割（経過時間で表記）

| 経過 | 時間 | やること |
|---|---|---|
| 0:00–0:30 | 30分 | **Q205 ネスト集約**（実装）。group-by × reduce で「グループごと sum」。計算量を声に出す＋「軸が増えたら？」自問 |
| 0:30–1:00 | 30分 | **Q210 多軸集約**（実装）。Q205 にエリア軸追加＝後出し要件の典型。多段 group-by / `(juxt :area :building/id)` |
| 1:00–1:30 | 30分 | **Q211 ネスト不変更新**（実装）。`update-in`/`assoc-in`/`update` で元を壊さず一部だけ書き換え |
| 1:30–1:40 | 10分 | 休憩 |
| 1:40–2:00 | 20分 | **小物を手に焼き付け**：`(into {} (map (juxt :id identity)) coll)` を数回手書き／`max-key`・`(sort-by f >)`+`take`・`fnil`・`frequencies`／`are` 表形式テスト `(are [in out] (= out (f in)) …)` を1個 |
| 2:00–2:20 | 20分 | **口頭 awareness（実装しない）**：グラフ「隣接map＋訪問setでBFS/DFS、最短はPersistentQueueで距離更新」／DP「再帰→memoize→ボトムアップ」／貪欲「終端でsort→非重複を貪欲選択」を各言えるように |
| 2:20–2:30 | 10分 | **Q212 目を通すだけ**：「素map＝軽量・動的／defrecord＝固定・型・性能／defprotocol＝多態」を1回声に出す |
| 2:30–3:00 | 30分 | **予備**：詰まった所の復習／別解で書き直し／余れば確認のみ(Q213/215/206) |

- 主役の3問（Q205/210/211）を最初の90分で確実に。算法は実装せず awareness 止まり

## 6/15(月) 昼 45分 — 自信のない所の自由復習（時間割は組まない）
- これまでやってきて**不安が残る所を自分で選んで**復習。スケジュールは設定しない
- 原則だけ: **新規には手を出さない** / 詰め込まない / 早めに切り上げて休む
- 引き出しの候補（やりたければ）: 定型句の音読 / `(into {} (map (juxt :id identity)) coll)` を数回書く / グラフ・DP・貪欲の方針を口頭で1回

## 6/16(火) 当日 — ★記事の3軸をそのまま振る舞いに
- 朝は何もしない。電車で定型句を眺める。13:10 会場着
- **①書く前に質問**（曖昧要件を整理・ヒアリング＝評価軸③）
- **②「まずナイーブで書きます」と宣言**して実装（評価軸①）
- **③計算量を口に出す**（「O(N²)、ハッシュで O(N) に落とせます」＝評価軸②）
- **④詰まったら遠慮なく相談**（面接官は敵でない）

---

## 確認のみ／skip（理由つき）
- **確認のみ（冗長）**: Q213(≒Q214 ハッシュ集合) / Q215(≒Q207 id結合) / Q206(≒Q205 count) … 既習と同思想で即興可
- **skip**: グラフ/DP/貪欲の**実装**（記事に算法分野の言及なし・実務寄り題材のため過剰）、Q216/Q217/Q218、Q222、空書きチェック、対話練習
- 方針の芯: estie は**業務寄り問題＋改善プロセス＋計算量説明＋曖昧要件の対処**。算法ドリルでなく**ドメイン操作の流暢さ**と**当日の振る舞い**で勝つ

## チェックリスト
- [x] Q036 / [x] Q037 / [x] Q042
- [x] **Q207** / [x] **Q208** / [x] **Q209** / [x] **Q214**（核心＝改善プロセス＋計算量説明）
- [ ] **Q205** ネスト集約（実装）  ← 主役
- [ ] **Q210** 多軸集約（実装）  ← 主役・後出し要件の典型
- [ ] **Q211** ネスト不変更新 update-in/assoc-in（実装）  ← 後出し要件の道具
- [ ] 小物を手に焼き付け: into/juxt・max-key・(sort-by f >)+take・fnil・frequencies・are(表形式テスト)
- [ ] 口頭 awareness: グラフ / DP / 貪欲（実装しない）
- [ ] Q212 目を通すだけ（口頭で判断基準）
- [ ] 確認のみ: Q213 / Q215 / Q206
- [ ] 最終調整（詰まった所＋定型句＋into/juxtドリル＋awareness維持）  ← 6/15
